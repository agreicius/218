<?xml version="1.0" encoding="UTF-8"?>

<section xml:id="sec_algebraic_func_II" xmlns:xi="http://www.w3.org/2001/XInclude">
    <title>Algebraic functions: continued</title>
    <introduction>
        <p>
            We now deepen our acquaintance with the different families of functions introduced in <xref ref="sec_algebraic_func"/>, treating questions of implied domain, range, and graph properties. 
        </p>
    </introduction>
    <subsection xml:id="ss_lo_alg">
    <title>Learning objectives</title>
    <p>
        <ul>
            <li>
                <p>
                    Determine the implied domain of power, polynomial and rational functions. 
                </p>
            </li>
            <li>
                <p>
                    Explore graph properties of power, polynomial, and rational functions
                </p>
            </li>
            <li>
                <p>
                    Factoring polynomials and finding roots
                </p>
            </li>
        </ul>
    </p>
    </subsection>
    <subsection xml:id="ss_power_func_II">
    <title>Power functions</title>
    <p>
        The properties of power functions are nicely summarized by their graphs. Let's look first at the graphs of power functions of the form <m>f(x)=x^{n}</m>, where <m>n\in \Z</m> is an integer. 
    </p>
    <figure xml:id="fig_power_positive_integer_powers">
        <caption>Power functions: positive integer power</caption>
        <sbsgroup width="22%">
            <sidebyside>
                <figure xml:id="fig_power_2">
                    <caption><m>f(x)=x^2</m></caption>
                    <image>
                        <shortdescription>Graph of x squared</shortdescription>
                        <sageplot>
                        f(x)=x^2
                        plot(f(x),x,-3,3, thickness=2)
                        </sageplot>
                    </image>

                </figure>
                <figure xml:id="fig_power_3">
                    <caption><m>f(x)=x^3</m></caption>
                    <image>
                        <shortdescription>Graph of x cubed</shortdescription>
                        <sageplot>
                        f(x)=x^3
                        plot(f(x),x,-3,3,thickness=2)
                        </sageplot>
                    </image>

                </figure>
                <figure xml:id="fig_power_4">
                    <caption><m>f(x)=x^4</m></caption>
                    <image>
                        <shortdescription>Graph of x to the fourth</shortdescription>
                        <sageplot>
                        f(x)=x^4
                        plot(f(x),x,-3,3,thickness=2)
                        </sageplot>
                    </image>

                </figure>
                <figure xml:id="fig_power_5">
                    <caption><m>f(x)=x^5</m></caption>
                    <image>
                        <shortdescription>Graph of x to the fifth</shortdescription>
                        <sageplot>
                        f(x)=x^5
                        plot(f(x),x,-3,3,thickness=2)
                        </sageplot>
                    </image>

                </figure>
            </sidebyside>
        </sbsgroup>
    </figure>
    <figure xml:id="fig_power_neg_integer_powers">
        <caption>Power functions: negative integer power</caption>
       
        <sbsgroup width="22%">
            <sidebyside>
                <figure xml:id="fig_power_neg2">
                    <caption><m>f(x)=x^{-2}</m></caption>
                    <image>
                        <shortdescription>Graph of x to minus 2</shortdescription>
                        <sageplot>
                        f(x)=x^-2
                        plot(f(x),x,-3,3, thickness=2,ymin=0,ymax=10,detect_poles=show)
                        </sageplot>
                    </image>

                </figure>
                <figure xml:id="fig_power_neg3">
                    <caption><m>f(x)=x^{-3}</m></caption>
                    <image>
                        <shortdescription>Graph of x to minus 3</shortdescription>
                        <sageplot>
                        f(x)=x^-3
                        plot(f(x),x,-3,3,thickness=2,ymin=-20,ymax=20,detect_poles=show)
                        </sageplot>
                    </image>

                </figure>
                <figure xml:id="fig_power_neg4">
                    <caption><m>f(x)=x^{-4}</m></caption>
                    <image>
                        <shortdescription>Graph of x to minus 4</shortdescription>
                        <sageplot>
                        f(x)=x^-4
                        plot(f(x),x,-3,3,thickness=2,ymin=0,ymax=10,detect_poles=show)
                        </sageplot>
                    </image>

                </figure>
                <figure xml:id="fig_power_neg5">
                    <caption><m>f(x)=x^{-5}</m></caption>
                    <image>
                        <shortdescription>Graph of x to minus 5</shortdescription>
                        <sageplot>
                        f(x)=x^-5
                        plot(f(x),x,-3,3,thickness=2,ymin=-200,ymax=200,detect_poles=show)
                        </sageplot>
                    </image>

                </figure>
            </sidebyside>
        </sbsgroup>
    </figure>
    <p>
      The eight graphs in <xref ref="fig_power_positive_integer_powers"/> and <xref ref="fig_power_neg_integer_powers"/> nicely illustrate how the precise nature of the power <m>n</m> involved affects the properties of <m>f(x)=x^n</m> and its graph. In particular, we see that the general shape of the graph is determined by whether <m>n</m> is positive or negative (the <em>sign</em> of <m>n</m>), and whether <m>n</m> is even or odd (the <em>parity</em> of <m>n</m>). <xref ref="table_power_integers"/> summarizes some observations about these power functions that are strongly suggested by our graphs. The claims there about domain are easily verified using the definition of power functions (see <xref ref="eg_power_implied_domain"/>). The claims about range can be shown to be true using <xref ref="th_power_identities"/>, as <xref ref="eg_power_func_range"/> illustrates. 
    </p>
    <table xml:id="table_power_integers">
        <title>Properties of <m>f(x)=x^n</m>, <m>n\in \Z</m></title>
        <tabular>
            <col halign="center"/><col halign="center"/><col halign="center"/><col halign="center"/>
            <row header="yes">
                <cell>Sign of <m>n</m></cell><cell>Parity of <m>n</m></cell><cell>Domain of <m>f</m></cell><cell>range of <m>f</m></cell>
            </row>
            <row>
                <cell><m>+</m></cell><cell>even</cell><cell><m>\R</m></cell><cell><m>[0,\infty)</m></cell>
            </row>
            <row>
                <cell><m>+</m></cell><cell>odd</cell><cell><m>\R</m></cell><cell><m>\R</m></cell>
            </row>
            <row>
                <cell><m>-</m></cell><cell>even</cell><cell><m>\R-\{0\}</m></cell><cell><m>(0,\infty)</m></cell>
            </row>
            <row>
                <cell><m>-</m></cell><cell>odd</cell><cell><m>\R-\{0\}</m></cell><cell><m>\R-\{0\}</m></cell>
            </row>
        </tabular>
    </table>
    <example xml:id="eg_power_func_range">
    <title>Power function: range</title>
    <statement>
    <p>
    Let <m>f(x)=x^n</m>, where <m>n</m> is a positive even integer. Show that <m>\range f=[0,\infty)</m>. 
    </p>
    </statement>
    <solution>
    <p>
    Since <m>n=2k</m> for some integer <m>k</m>, we have 
    <md>
        <mrow>f(x) \amp =x^{2k}</mrow>
        <mrow> \amp =(x^k)^2 \amp (\text{iter. powers})
        </mrow>
        <mrow> \amp \geq 0 </mrow>
    </md>,
    where the last inequality follows from the observation that the square of any real number is nonnegative. Since the outputs of <m>f</m> are all nonnegative, we conclude <me>\range f\subseteq [0,\infty)</me>. 
    We now set about proving the reverse inclusion: <m>[0,\infty)\subseteq \range f</m>. To this end, take any <m>b\in [0,\infty)</m>. Since <m>r\geq 0</m>, the number <m>r^{1/n}=\sqrt[n]{r}</m> is well defined. (In this case, since <m>n</m> is even, <m>r^{1/n}</m> by definition is the unique nonnegative <m>n</m>-th root of <m>r</m>.) Setting <m>a=r^{1/n}</m>, we have 
    <md>
        <mrow>f(a) \amp =a^{n}</mrow>
        <mrow> \amp =\left(r ^{1/n}\right)^n</mrow>
        <mrow> \amp = r^{\tfrac{1}{n}\cdot n} \amp (\text{iterated powers})</mrow>
        <mrow> \amp =r^1</mrow>
        <mrow> \amp =r</mrow>
    </md>.
    This shows that <m>r</m> is an output of <m>f</m>, and thus that <m>r\in \range f</m>, as desired. We conclude that <m>\range f=[0,\infty)</m>. 
    </p>
    </solution>
    </example>
    <p>
        Power functions of the form <m>f(x)=x^{1/n}=\sqrt[n]{x}</m>, where <m>n</m> is positive, also exhibit some easy to identify distinctive properties. We call these functions <em><m>n</m>-th root functions</em>.
    </p>
    <figure xml:id="fig_nth_roots">
        <caption>Power functions: <m>n</m>-th roots</caption>
        <sbsgroup width="45%">
            <sidebyside>
                <figure xml:id="fig_square_root">
                    <caption><m>f(x)=x^{1/2}=\sqrt{x}</m></caption>
                    <image>
                        <shortdescription>Square-root function graph</shortdescription>
                        <sageplot>
                        f(x)=sqrt(x)
                        plot(f(x),x,0,10,thickness=2,ymin=0, ymax=3.5)
                        </sageplot>
                    </image>
                </figure>
                <figure xml:id="fig_third_root">
                    <caption><m>f(x)=x^{1/3}=\sqrt[3]{x}</m></caption>
                    <image>
                        <shortdescription>Cube-root function graph</shortdescription>
                        <sageplot>
                        f(x)=real_nth_root(x, 3)
                        plot(f(x),x,-10,10,thickness=2,ymin=-3, ymax=3)
                        </sageplot>
                    </image>
                </figure>
                </sidebyside>
            
               <sidebyside>
                 <figure xml:id="fig_fourth_root">
                    <caption><m>f(x)=x^{1/4}=\sqrt[4]{x}</m></caption>
                    <image>
                        <shortdescription>Fourth-root function graph</shortdescription>
                        <sageplot>
                        f(x)=real_nth_root(x, 4)
                        plot(f(x),x,0,10,thickness=2,ymin=0, ymax=3.5)
                        </sageplot>
                    </image>
                </figure>
                <figure xml:id="fig_fifth_root">
                    <caption><m>f(x)=x^{1/5}=\sqrt[5]{x}</m></caption>
                    <image>
                        <shortdescription>Fifth-root function graph</shortdescription>
                        <sageplot>
                        f(x)=real_nth_root(x, 5)
                        plot(f(x),x,-10,10,thickness=2, ymin=-3, ymax=3)
                        </sageplot>
                    </image>
                </figure>
            </sidebyside>
        </sbsgroup>
    </figure>
    <p>
        Again, we summarize some properties of these functions that the graphs make evident. In this case, since <m>n</m> is always positive, it is only the parity (even/odd) of <m>n</m> that governs the general shape of the graph. 
    </p>
    <table>
        <title>Properties of <m>f(x)=x^{1/n}=\sqrt[n]{x}</m></title>
        <tabular>
            <col halign="center"/><col halign="center"/><col halign="center"/>
            <row header="yes">
                <cell>Parity of <m>n</m></cell><cell>Domain of <m>f</m></cell><cell>Range of <m>f</m></cell>
            </row>
            <row>
                <cell>Even</cell><cell><m>[0,\infty)</m></cell><cell><m>[0,\infty)</m></cell>
            </row>
            <row>
                <cell>Odd</cell><cell><m>\R</m></cell><cell><m>\R</m></cell>
            </row>

        </tabular>
    </table>
    <example xml:id="eg_modeling_power">
    <title>Modeling with power functions</title>
    <statement>
    <p>
        Each graph in <xref ref="fig_eg_model_power"/> is the graph of a function <m>f</m> of the form <m>f(x)=x^q</m>, <m>q\in \Q</m> is given. Find <m>q</m>.
    </p>
    <figure xml:id="fig_eg_model_power">
        <caption>Modeling with power functions</caption>
    <sidebyside>
        <figure>
            <caption></caption>
            <image>
                <shortdescription>Mystery graph of power function</shortdescription>
               <sageplot>
               plot(real_nth_root(x,5), (x, -40, 40),gridlines=([32],[2]),thickness=2) + point((32, 2), size=50, color='red')
               </sageplot> 
            </image>
        </figure>
        <figure>
            <caption></caption>
            <image>
                <shortdescription>Mystery graph of power function</shortdescription>
               <sageplot>
               plot(x^-2, (x, -2,2),ymax=6,gridlines=([-1/2],[4]),thickness=2) + point((-1/2, 4), size=50, color='red')
               </sageplot> 
            </image>
        </figure>
    </sidebyside>
    </figure>
    </statement>
    <solution>
    <p>
        Although not strictly necessary for solving this exercise, looking at <xref first="fig_power_positive_integer_powers" last="fig_nth_roots"/>, we see that the function graphed in (a) should be of the form <m>f(x)=x^{1/n}</m>, where <m>n</m> is a positive odd numer, and the function graphed in (b) should be of the form <m>f(x)=x^{-n}</m>, where <m>n</m> is a positive even number. 
    <ol marker="(a)">
        <li>
            <p>
                Our function <m>f</m> satisfies <m>f(x)=x^q</m> for all <m>x</m> and <m>f(32)=2</m>. This implies that 
                <me>
                    32^q=2
                </me>.
                Later, once we introduce logarithmic functions, we will have a systematic way of solving for <m>q</m>. Instead, using our observation at the top that <m>q=1/n</m> for some positive odd number <m>n</m>, and doing some guess-and-check, we see that <m>q=1/5</m> works. Thus <m>f(x)=x^{1/5}</m>. 
            </p>
        </li>
        <li>          
            <p>
            Now our function <m>f</m> satisfies <m>f(x)=x^q</m> and <m>f(-1/2)=4</m>: <ie/>, <m>(-1/2)^q=4</m>. Again, being guided by our observation at the top that <m>q=-n</m> for some positive even integer <m>n</m>, and trying out the choices <m>n=2,4,\dots</m>, we see that <m>q=2</m> works. We conclude that <m>f(x)=x^{-2}</m>.  
            </p>
        </li>
    </ol>
    </p>
    </solution>
    </example>
    
    </subsection>
    <subsection xml:id="ss_polynomials">
    <title>Polynomials</title>
    <p>
    In contrast to the simple power functions considered above, the behavior of polynomials can be quite complicated, and as a general rule becomes more and more complicated as the degree of the polynomial increases. This is illustrated in <xref ref="fig_polynomials"/>. 
    </p>
    <figure xml:id="fig_polynomials">
          <caption>Polynomials of increasing degree.</caption>
            <sbsgroup width="40%">
              <sidebyside>
            <figure xml:id="fig_cubic">
              <caption><m>f(x)=x^3+x^2-2x</m></caption>
              <image>
                <shortdescription>Graph of cubic</shortdescription>
                <sageplot>
                f(x)=x^3+x^2-2*x
                plot(f(x),-3,3,thickness=2,ymin=-6,ymax=10)
                </sageplot>
              </image>
            </figure>
                <figure xml:id="fig_quart">
              <caption><m>f(x)=x^4+x^3-2x^2</m></caption>
              <image>
                <shortdescription>Graph of quartic</shortdescription>
                <sageplot>
                f(x)=x^4+x^3-2*x^2
                plot(f(x),-3,3,thickness=2,ymin=-6,ymax=10)
                </sageplot>
              </image>
            </figure>
            </sidebyside>
            <sidebyside>
              <figure xml:id="fig_quint">
              <caption><m>f(x)=x^5-x^4-4x^3+4x^2</m></caption>
              <image>
                <shortdescription>Graph of quintic</shortdescription>
                <sageplot>
                f(x)=x^5-x^4-4*x^3+4*x^2
                plot(f(x),-3,3,thickness=2,ymin=-6,ymax=10)
                </sageplot>
              </image>
            </figure>
          </sidebyside>
        </sbsgroup>
        </figure>
    <p>
        We see that as the degree of the polynomials increases, the graphs of the functions tend to have more <q>turning points</q> (what we will later define as <em>local maxima</em> and <em>local minimima</em>). Furthermore, it is not clear from these examples how the range of a polynomial <m>f(x)=\anpoly</m> is determined by its degree and/or coefficients. Once again, calculus will provide us with the theoretical tools to answer questions related to these issues. For now, we will address another important question: how do we find the roots of a polynomial? Looking at the <m>x</m>-intercepts of the graphs in <xref ref="fig_polynomials"/>, it appears that the number of roots of a polynomial has something to do with its degree. The following theorem gives a precise (if somewhat incomplete) articulation of this relationship. 
    </p>
    <theorem xml:id="th_poly_roots">
    <title>Roots of polynomials</title>
    <statement>
    <p>
    Let <m>f(x)</m> be a polynomial of positive degree <m>n</m>. 
    <ol>
        <li>
            <p>
                <m>f</m> has at most <m>n</m> distinct roots. 
            </p>
        </li>
        <li>
            <p>
                A real number <m>a\in \R</m> is a root of <m>f</m> if and only if <m>f</m> factors as 
                <me>
                    f(x)=(x-a)g(x)
                </me>
                for some polynomial <m>g(x)</m>.
            </p>
        </li>
        
    </ol>
    </p>
    </statement>
    </theorem>
    <p>
       Statement (1) of <xref ref="th_poly_roots"/> tells us that the degree of a polynomial bounds the number of possible real roots, and thus also the number of possible <m>x</m>-intercepts. Thus, a quadratic polynomial has at most two roots, a cubic polynomial has at most three roots, a quartic polynomial has at most four roots, <etc/>. Note that the theorem does <em>not</em> claim that a degree-<m>n</m> polynomial must have <m>n</m> roots. Indeed we have seen already examples of quadratic polynomials (degree-2) that have no (real) roots!  
    </p>
    <aside>
        <p>
            If we move from <m>\R</m> to the larger number system <m>\C</m> of complex numbers, then we can strengthen (1) of <xref ref="th_poly_roots"/>: namely, every polynomial of positive degree <m>n</m> has <m>n</m> <em> roots</em> (counting multiplicity).  This is the content of the <em>fundamental theorem of algebra</em>. The crucial detail here is the modifier <q>complex</q> in <q>complex root</q>.
        </p>
    </aside> 
    
    <p>
        Statement (2) of <xref ref="th_poly_roots"/> makes the connection between roots of polynomials and factorization. As a result, the more we are able to factor a polynomial, the more we can say about its roots; conversely, every time we find a root of polynomial <m>f</m>, we can make progress on the factorization of <m>f</m> by <q>factoring out</q> that root. Factoring polynomials is thus an important skill, and we gather here some techniques for doing this. 
    </p>
    <algorithm xml:id="proc_poly_division">
        <title>Polynomial long division</title>
        <statement>
            <p>
                <ol><li>
                    <p>
                Let <m>f</m> and <m>g</m> be polynomials of positive degree. There are unique polynomials <m>q(x)</m> and <m>r(x)</m> satisfying 
                <mdn>
                    <mrow xml:id="eq_div_alg"> f(x)\amp = g(x)q(x)+r(x) \amp \deg r(x)&lt; \deg g </mrow>
                </mdn>. 
                We call <m>q</m> and <m>r</m> the <term>quotient</term> and <term>remainder</term> upon division of <m>f</m> by <m>g</m>. 
            </p>
            <p>
                The process of computing <m>q(x)</m> and <m>r(x)</m> is called <term>polynomial longdivision</term> of <m>f</m> by <m>g</m>. We illustrate this process in <xref ref="eg_poly_long_div"/>. See also <xref ref="table_poly_div"/>
            </p>
                </li>
                <li>
                    <p>
                        A real number <m>a</m> is a root of the polynomial <m>f(x)</m> if and only if the remainder upon dividing <m>f(x)</m> by <m>x-a</m> is the zero polynomial. In this case we have <m>r(x)=0</m> and <m>f(x)=(x-a)q(x)</m>. 
                    </p>
                </li>
            </ol>
            </p>
        </statement>
    </algorithm>
    <example xml:id="eg_poly_long_div">
    <title>Polynomial long division</title>
    <statement>
    <p>
    Find the quotient <m>q(x)</m> and remainder <m>r(x)</m> upon dividing <m>f(x)=3x^4+2x^2+7x+1</m> by <m>g(x)=x^2+x+1</m>.
    </p>
    </statement>
    <solution>
    <p>
You may remember that long division of <m>f</m> by <m>g</m> proceeds iteratively, where at each step we multiply <m>g</m> by a monomial <m>cx^m</m> in order to match the leading term of the polynomial being divided. We spell this out in more detail in the steps below, but you are probably more accustomed to steps in this process being shown as in <xref ref="table_poly_div"/>, and indeed that is the way you should perform this operation. Consider the following as just an in-depth description of what we are actually doing in that process. 
    <ul>
        <li>
            <p>
                Multiply <m>g</m> by a monomial <m>cx^n</m> so that the leading terms of <m>f</m> and <m>(cx^n)g(x)</m> match. In this case we choose the monomial <m>3x^2</m>.
            </p>
            <p>
                Set our <em>temporary quotient</em> <m>q(x)</m> as <m>3x^2</m> and our <em>temporary remainder</em> <m>r(x)</m> as 
                <md>
                    <mrow>r(x) \amp = f(x)-3x^2\cdot g(x)</mrow>
                    <mrow> \amp =f(x)-3x^2(x^2+x+1)</mrow>
                    <mrow> \amp =-3x^3-x^2+7x</mrow>
                </md>.
            </p>
        </li>
        <li>
            <p>
               If <m>\deg r</m> is not less than <m>\deg g</m> for our temporary remainder  <m>r(x)</m>, repeat Step 1 with <m>g(x)</m> and <m>r(x)</m> (instead of <m>g(x)</m> and <m>f(x)</m>). <em>Update</em> our temporary quotient <m>q(x)</m> by adding to it the relevant monomial <m>cx^m</m>, and <em>update</em> our temporary remainder <m>r(x)</m> by subtracting <m>(cx^m)g(x)</m> from it. 
            </p>
            <p>
                In the present case, since 
                <me>
                    \deg r(x)=\deg -3x^3-x^2+7x-3\geq \deg f=2 
                </me>,
                we multiply <m>g(x)</m> by the monomial <m>-3x</m> in order to match the leading term of <m>-3x^3-x^2+7x-3</m>, and update our <m>q(x)</m> and <m>r(x)</m> as follows: 
                <md>
                    <mrow>q(x) \amp = 3x^2-3x</mrow>
                    <mrow>r(x) \amp = -3x^3-x^2+7x-3-(-3x)g(x)</mrow>
                    <mrow> \amp =-3x^3-x^2+7x-3-(-3x)(x^2+x+1) </mrow>
                    <mrow> \amp = 2x^2+10x+1</mrow>
                </md>
            </p>
        </li>
        <li>
            <p>
                Continue this updating process until <m>\deg r&lt; \deg g(x)</m>. 
            </p>
            <p>
                In our case, we need to do one more iteration. To match the leading terms, we multiply <m>g(x)</m> by the monomial <m>2</m>. Updating our <m>q(x)</m> and <m>r(x)</m> yields
                <md>
                    <mrow>q(x) \amp =3x^2-3x+2</mrow>
                    <mrow>r(x) \amp = 2x^2+10x+1-2g(x)</mrow>
                    <mrow> \amp = 2x^2+10x+1-2(x^2+x+1)</mrow>
                    <mrow> \amp = 8x-1</mrow>
                </md>.
                Since <m>\deg 8x-1=1&lt; \deg g=2</m>, we are done. We conclude that <m>f(x)=g(x)q(x)+r(x)</m>, where 
                <md>
                    <mrow>q(x) \amp =3x^2-3x+2</mrow>
                    <mrow>r(x) \amp =8x-1</mrow>
                </md>.
            </p>
        </li>
    </ul>
    </p>
    </solution>
    </example>
    
    <table xml:id="table_poly_div">
        <title>Polynomial long division</title>
        <tabular>
            <col halign="right"/><col halign="left" /><col halign="left" /><col halign="left" /><col halign="left" />
            <col halign="left" />
            <row>
                <cell></cell><cell bottom="medium"></cell><cell bottom="medium"></cell><cell bottom="medium"><m>3x^2</m></cell><cell bottom="medium"><m>-3x</m></cell><cell bottom="medium"><m>+2</m></cell>
            </row>
            <row>
                <cell><m>x^2+x+1)</m></cell><cell ><m>\phantom{-(}3x^4</m></cell><cell ></cell><cell ><m>+2x^2</m></cell><cell><m>+7x</m></cell><cell><m>+1</m></cell>
            </row>
            <row>
                <cell></cell><cell bottom="medium"><m>-(3x^4</m></cell ><cell bottom="medium"><m>+3x^3</m></cell><cell bottom="medium"><m>+3x^2)</m></cell><cell></cell><cell></cell>
            </row>
            <row>
                <cell></cell><cell></cell><cell><m>\phantom{-(}-3x^3</m></cell><cell><m>-x^2</m></cell><cell><m>+7x</m></cell><cell></cell>
            </row>
            <row>
                <cell></cell><cell></cell><cell bottom="medium"><m>-(-3x^3</m></cell><cell bottom="medium"><m>-3x^2</m></cell><cell bottom="medium"><m>-3x)</m></cell><cell></cell>
            </row>
            <row>
                <cell></cell><cell></cell><cell></cell><cell><m>\phantom{-(}2x^2</m></cell><cell><m>+10x</m></cell><cell><m>+1</m></cell>
            </row>
            <row>
                <cell></cell><cell></cell><cell></cell><cell bottom="medium"><m>-(2x^2</m></cell><cell bottom="medium"><m>+2x</m></cell><cell bottom="medium"><m>+2)</m></cell>
            </row>
            <row>
                <cell></cell><cell></cell><cell></cell><cell></cell><cell><m>8x</m></cell><cell><m>-1</m></cell>
            </row>
        </tabular>
    </table>
    <example xml:id="eg_poly_root_check">
    <title>Polynomial root check</title>
    <statement>
    <p>
    Let <m>f(x)=x^3+4x^2+4x+3</m>. For each <m>a\in \R</m>, decide whether <m>a</m> is a root by computing <m>f(a)</m>, then verify your answer using <xref ref="proc_poly_division"/>.
    <ol>
        <li>
            <p>
                <m>a=-1</m>
            </p>
        </li>
        <li>
            <p>
                <m>a=-3</m>
            </p>
        </li>
    </ol>
    </p>
    </statement>
    <solution>
    <p>
    <ol>
        <li>
            <p>
                Since
                <me>
                    f(-1)=-1+4-4+3=2
                </me>,
                we see that <m>a=-1</m> is not a root of <m>f</m>. According to <xref ref="proc_poly_division"/> this means that dividing <m>f(x)</m> by <m>x+1</m> should result in a <em>nonzero</em> remainder polynomial <m>r(x)</m>. We verify by performing longdivision:
            </p>
            <tabular>
            <col halign="right"/><col halign="right" /><col halign="right" /><col halign="right" /><col halign="right" />
            
            <row>
                <cell></cell><cell bottom="medium"></cell><cell bottom="medium"><m>x^2</m></cell><cell bottom="medium"><m>+3x</m></cell><cell bottom="medium"><m>+1</m></cell>
            </row>
            <row>
                <cell><m>x+1)</m></cell><cell ><m>\phantom{-(}x^3</m></cell><cell ><m>+4x^2</m></cell><cell ><m>+4x</m></cell><cell><m>+3</m></cell>
            </row>
            <row>
                <cell></cell><cell bottom="medium"><m>-(x^3</m></cell ><cell bottom="medium"><m>+x^2)</m></cell><cell ><m></m></cell><cell></cell>
            </row>
            <row>
                <cell></cell><cell ><m></m></cell ><cell ><m>\phantom{-(}3x^2</m></cell><cell ><m>+4x</m></cell><cell>+3</cell>
            </row>
            <row>
                <cell></cell><cell><m></m></cell ><cell bottom="medium" ><m>-(3x^2</m></cell><cell bottom="medium" ><m>+3x)</m></cell><cell></cell>
            </row>
            <row>
                <cell></cell><cell><m></m></cell ><cell></cell><cell ><m>\phantom{-(}x</m></cell><cell  ><m>+3</m></cell>
            </row>
            <row>
                <cell></cell><cell><m></m></cell ><cell></cell><cell bottom="medium" ><m>-(x</m></cell><cell bottom="medium" ><m>+1)</m></cell>
            </row>
            <row>
                <cell></cell><cell><m></m></cell ><cell></cell><cell></cell><cell>2</cell>
            </row>
        </tabular>
            <p>    
                Thus diving <m>f</m> by <m>x+1</m> yields a quotient of <m>q(x)=x^2+3x+1</m> and remainder of <m>r(x)=2</m>: <ie/>, 
                <me>
                    x^3+4x^2+4x+3=(x+1)\underset{q(x)}{(x^2+3x+1)}+\underset{r(x)}{2}
                </me>.
                Since <m>r(x)=2</m> is not the zero polynomial we conclude (again) that <m>a=-1</m> is not a root.
            </p>
        </li>
        <li>
            <p>
                Similarly, we compute 
                <me>
                    f(-3)=-27+36-12+3=0
                </me>,
                showing that <m>a=-3</m> is a root of <m>f</m>. Now perform longdivision of <m>f</m> by <m>x+3</m>:
            </p>
            <tabular>
            <col halign="right"/><col halign="right" /><col halign="right" /><col halign="right" /><col halign="right" />
            
            <row>
                <cell></cell><cell bottom="medium"></cell><cell bottom="medium"><m>x^2</m></cell><cell bottom="medium"><m>+x</m></cell><cell bottom="medium"><m>+1</m></cell>
            </row>
            <row>
                <cell><m>x+3)</m></cell><cell ><m>\phantom{-(}x^3</m></cell><cell ><m>+4x^2</m></cell><cell ><m>+4x</m></cell><cell><m>+3</m></cell>
            </row>
            <row>
                <cell></cell><cell bottom="medium"><m>-(x^3</m></cell ><cell bottom="medium"><m>+3x^2)</m></cell><cell ><m></m></cell><cell></cell>
            </row>
            <row>
                <cell></cell><cell ><m></m></cell ><cell ><m>\phantom{-(}x^2</m></cell><cell ><m>+4x</m></cell><cell>+3</cell>
            </row>
            <row>
                <cell></cell><cell><m></m></cell ><cell bottom="medium" ><m>-(x^2</m></cell><cell bottom="medium" ><m>+3x)</m></cell><cell></cell>
            </row>
            <row>
                <cell></cell><cell><m></m></cell ><cell></cell><cell ><m>\phantom{-(}x</m></cell><cell  ><m>+3</m></cell>
            </row>
            <row>
                <cell></cell><cell><m></m></cell ><cell></cell><cell bottom="medium" ><m>-(x</m></cell><cell bottom="medium" ><m>+3)</m></cell>
            </row>
            <row>
                <cell></cell><cell><m></m></cell ><cell></cell><cell></cell><cell>0</cell>
            </row>
        </tabular>
        <p>
            Since <m>r(x)=0</m> is the zero polynomial, we conclude (again) that <m>a=-3</m> is a root, and furthermore the quotient <m>q(x)=x^2+x+1</m> yields the factorization 
        <me>
            x^3+4x^2+4x+1=(x+3)(x^2+x+1)
        </me>.
            </p>
        </li>
    </ol>
    </p>
    </solution>
    </example>
    <p>
        You may have noticed that the two candidate roots of the polynomial in <xref ref="eg_poly_root_check"/> were integer factors of its constant term. As it turns out, given a polynomial with <em>integer coefficients</em>, any <em>integer root</em> of that polynomial must be a factor of its constant term. 
    </p>
    <theorem xml:id="th_int_root">
    <title>Integer root theorem</title>
    <statement>
    <p>
    Let <me>f(x)=\anpoly</me> and assume that all the coefficients of <m>f</m> are integers: <ie/>, <m>a_k\in \Z</m> for all <m>0\leq k\leq n</m>. Any integer root of <m>f</m> must be a factor of <m>a_0</m>: <ie/>, if <m>f(c)=0</m> and <m>c\in \Z</m>, then <m>c</m> is a factor of <m>a_0</m>.
    </p>
    </statement>
    </theorem>
    <p>
        Note the crucial detail in <xref ref="th_int_root"/>: an <em>integer</em> root of a polynomial with integer coefficients must divide the constant term. The theorem says nothing about possible non-integer roots, as the next example illustrates. 
    </p>
    <example xml:id="eg_no_integer_roots">
    <title>Integer roots</title>
    <statement>
    <p>
    Let <m>f(x)=x^2-2</m>. 
    <ol>
        <li>
            <p>
                Find all <em>integer</em> roots of <m>f</m>. 
            </p>
        </li>
        <li>
            <p>
                Find all roots of <m>f</m>. 
            </p>
        </li>
    </ol>
    </p>
    </statement>
    <solution>
    <p>
    <ol>
        <li>
            <p>
                Since <m>f</m> has integer coefficents, if <m>c</m> is an integer root of <m>f</m>, then it must be a factor of its constant term <m>-2</m>, according to <xref ref="th_int_root"/>. Thus the only possible <em>integer</em> roots of <m>f</m> are <m>\pm 1, \pm 2</m>. But <m>f(\pm 1)=-1</m> and <m>f(\pm 2)=2</m>. Thus none of these candidates is a root, showing <m>f</m> has no integer roots. 
            </p>
        </li>
        <li>
            <p>
                Since <m>f</m> is a quadratic function, it is easy to determine its roots. As usual, there are multiple ways of doing this, but in this case the easiest method would be to proceed as follows:
                <md>
                    <mrow>f(x) \amp =0</mrow>
                    <mrow>x^2-2 \amp =0</mrow>
                    <mrow>x^2 \amp =2</mrow>
                    <mrow>x \amp =\pm \sqrt{2}</mrow>
                </md>.
                Thus, although <m>f</m> has no <em>integer</em> roots, its has the two real roots <m>\pm \sqrt{2}</m>. 
            </p>
        </li>
    </ol>
    </p>
    </solution>
    </example>
    <p>
        Once we find a root <m>x=a</m> of a polynomial <m>f(x)</m>, the resulting factorization <m>f(x)=(x-a)q(x)</m> allows us to find all further roots by continuiung the process on <m>q(x)</m>, as the next example illustrates. 
    </p>
    <example xml:id="eg_poly_roots">
    <title>Polynomial roots</title>
    <statement>
    <p>
    Find all roots of <m>f(x)=x^3+4x^2+4x+3</m>.
    </p>
    </statement>
    <solution>
    <p>
    From <xref ref="eg_poly_root_check"/>, we know that <m>f</m> factors as 
    <me>
        f(x)=(x+3)(x^2+x+1)
    </me>, 
    and thus that <m>-3</m> is a root of <m>f</m>. Are there any other roots? Suppose <m>a\ne -3</m>. Evaluating <m>f</m> at <m>a</m> yields 
    <me>
        f(a)=(a+3)(a^2+a+1)
    </me>.
    Since <m>a\ne -3</m>, <m>a+3\ne 0</m>. By <xref ref="th_zero_divisors"/>, we have <m>f(a)=0</m> if and only if <m>a^2+a+1=0</m>. Thus, <m>a\ne -3</m> is a root of <m>f</m> if and only if it is a root of <m>g(x)=x^2+x+1</m>. But using <xref ref="th_quadratic_formula"/>, we see that the quadratic function <m>g(x)</m> has not roots! We conclude that <m>-3</m> is the only root of <m>f</m>. 
    </p>
    </solution>
    </example>
    <p>
        Let's make official the method and reasoning in this last example. 
    </p>
    <algorithm xml:id="proc_fact_roots">
        <title>Factoring polynomials and finding roots</title>
        <statement>
            <p>
            To find all roots of a polynomial <m>f</m>, you may proceed as follows. 
            <ol>
                <li>
                    <p>
                        Factor <m>f(x)</m> as best you can as a product of polynomials of smaller degree:
                        <men xml:id="eq_f_factored">
                            f(x)=f_1(x)f_2(x)\cdots f_r(x)
                        </men>.
                    </p>
                </li>
                <li>
                    <p>
                        A real number <m>a</m> is a root of <m>f</m> if and only if it is a root of one of the polynomials <m>f_k(x)</m>. Thus by finding all roots of each polynomial <m>f_k(x)</m>, we find all roots of <m>f</m>. 
                    </p>
                </li>
            </ol>
        </p>
        </statement>
    </algorithm>
    <p>
        Note that the success <xref ref="proc_fact_roots"/> depends on our ability to find all the roots of the factor polynomials <m>f_k(x)</m>. Accordingly, the smaller the degree of the <m>f_k</m>, the more likely we are in being able to find all roots of <m>f</m>. In particular, we will be home free if we are able to factor <m>f</m> far enough so that all factor polynomials are of degree one or two. 
    </p>
    <example xml:id="eg_roots_quartic">
    <title>Roots of a quartic</title>
    <statement>
    <p>
    Find all roots of <m>f(x)=x^4-4x^3+5x^2-4x+4</m>.
    </p>
    </statement>
    <solution>
    <p>
    Since the coefficients of <m>f</m> are integers, we first try and find an integer root of <m>f</m> using <xref ref="th_int_root"/>. Evaluating <m>f(a)</m> for <m>a=\pm 1, \pm 2, \pm 4</m> (the factors of the constant term of <m>f</m>), we see that <m>f(2)=0</m>, and thus that <m>(x-2)</m> is a factor of <m>f</m>. Using polynomial longdivision (we skip the steps here), we see that 
    <me>
        f(x)=(x-2)(x^3-2x^2+x-2)
    </me>.
    Now continue the process by factoring <m>g(x)=x^3-2x^2+x-2</m>. Again using <xref ref="th_int_root"/>, we try <m>\pm 1, \pm 2</m> as potential roots. It turns out that <m>g(2)=0</m>, and thus <m>x-2</m> can be factored out (again). Using polynomial longdivision (again we omit the steps), we find that 
    <me>
        g(x)=(x-2)(x^2+1)
    </me>,
    and thus that 
    <me>
        f(x)=(x-2)g(x)=(x-2)(x-2)(x^2+1)=(x-2)^2(x^2+1)
    </me>.
    Lastly, we apply <xref ref="th_quadratic_formula"/> to the remaining factor <m>x^2+1</m>, concluding that it has no roots. By <xref ref="proc_fact_roots"/>, we conclude that the only root of <m>f</m> is <m>2</m>. 
    </p>
    </solution>
    </example>
    <remark>
        <title>Multiplicity of a root</title>
        <p>
            The factorization 
            <me>
              f(x)=(x-2)^2(x^2+1)
            </me>
            from <xref ref="eg_roots_quartic"/> tells us that <m>2</m> is a <em>repeated root</em> of <m>f</m> of <em>multiplicity</em> two. In general, if a polynomial <m>f</m> factors as
            <me>
                f(x)=(x-a)^mq(x)
            </me>,
            where <m>a</m> is not a root of <m>q</m> (<ie/>, <m>q(a)\ne 0</m>), then <m>a</m> is called a <term>root of multiplicity <m>m</m></term>. For example, the polynomial
            <me>
                f(x)=x^2(x+1)(x-6)^4(x^2+1)
            </me>
            has a double root (<ie/>, root of multipicity two) at <m>x=0</m>, a simple root (<ie/>, root of multiplicity one) at <m>x=-1</m>, and a quadruple root at <m>x=6</m>. From the factorization, we know further that <m>f</m> has no additional roots, since <m>x^2+1</m> has not roots. 
        </p>
        <p>
            As we will see later in the course, the multiplicity of a root provides further nuanced information about how a polynomial <m>f</m> behaves near a the root. 
        </p>
    </remark>
    <p>
        Although we have been focusing on factoring polynomials by finding roots, this is not always the easiest way to proceed. Indeed, there are polynomials that can be factored nontrivially and yet have no roots. For example, the polynomial <m>f(x)=x^4+2x^2+1</m> factors as 
        <me>
            x^4+2x^2+1=(x^2+1)(x^2+1)
        </me>.
        <xref ref="th_factoring_tools"/> provides a couple of tricks for factoring polynomials that sometimes come in handy when we cannot easily find roots. The factorization above can be thought of an instance of (2) of <xref ref="th_factoring_tools"/>. We have 
        <me>
            x^4+2x^2+1=(x^2)^2+2(x^2)+1=g(x^2)
        </me>,
        where <m>g(x)=x^2+2x+1</m>. Since <m>g(x)</m> factors as
        <me>
            g(x)=(x+1)(x+1)
        </me>,
        we see that <m>f(x)=g(x^2)</m> factors as 
        <me>
            f(x)=g(x^2)=(x^2+1)(x^2+1)
        </me>.
    </p>
    <theorem xml:id="th_factoring_tools">
    <title>Factoring tools</title>
    <statement>
    <p>
    <ul>
        <li>
            <title>Difference of powers</title>
            <p>
                For all <m>a,b\in \R</m>, and all integers <m>n\geq 2</m>, we have 
                <men xml:id="eq_diff_powers">
                    a^n-b^n=(a-b)(a^{n-1}+a^{n-2}b+\cdots+ab^{n-2}+b^{n-1})
                </men>.
                In particular, we have
                <md>
                    <mrow>a^2-b^2 \amp =(a-b)(a+b) </mrow>
                    <mrow>a^3-b^3 \amp =(a-b)(a^2+ab+b^2)</mrow>
                    <mrow>a^4-b^4 \amp =(a-b)(a^3+a^2b+ab^2+b^2)</mrow>
                </md>.
            </p>
        </li>
        <li>
            <title>Polynomial expression of <m>x^m</m></title>
            <p>
                If we can write a polynnomial <m>f(x)</m> as 
                <me>
                    f(x)=g(x^m)
                </me>
                for some polynomial <m>g</m>, then a factorization 
                <me>
                    g(x)=p(x)q(x)
                </me>
                of <m>g</m> gives rise to the factorization 
                <me>
                    f(x)=p(x^m)q(x^m)
                </me>.
            </p>
        </li>
    </ul>
    </p>
    </statement>
    </theorem>
    <p>
        Factoring polynomials turns out to be our main tool for solving general polynomial equations, as we detail in <xref ref="proc_solve_poly"/>. 
    </p>
    <algorithm xml:id="proc_solve_poly">
        <title>Solving polynomial equations</title>
        <statement>
            <p>
                Consider a polynomial equation of the form 
                <men xml:id="eq_solve_poly_orig">
                    \anpoly =b
                </men>.
                To solve this equation for <m>x</m>, proceed as follows. 
                <ol>
                    <li>
                        <p>
                            Use algebra to bring all terms to the left side (using addition/subtraction), obtaining an equation of the form 
                            <men xml:id="eq_solve_poly">
                                a_nx^n+a_{n-1}x^{n-1}+\cdots +(a_0-b)=0
                            </men>.
                        </p>
                    </li>
                    <li>
                        <p>
                            Factor the polynomial on the left side of <xref ref="eq_solve_poly"/> as much as possible to obtain an equation of the form
                            <men xml:id="eq_solve_poly_factor">
                                f_1(x)f_2(x)\cdots f_r(x)=0
                            </men>.
                        </p>
                    </li>
                    <li>
                        <p>
                            The solutions of the original equation <xref ref="eq_solve_poly_orig"/> are precisely the roots of the polynomials <m>f_1,f_2,\dots, f_r</m>. 
                        </p>
                    </li>
                </ol>
            </p>
        </statement>
    </algorithm>
    <example xml:id="eg_solve_poly">
    <title>Solving a polynomial equation</title>
    <statement>
    <p>
    Find all solutions to the equation 
    <me>
        x^3-2x+3=x+1
    </me>.
    Follow <xref ref="proc_solve_poly"/>.
    </p>
    </statement>
    <solution>
    <p>
    First bring the equation in to the form <xref ref="eq_solve_poly"/>:
    <md>
        <mrow> x^3-2x+3\amp =x+1\amp </mrow>
        <mrow>x^3-3x+2 \amp = 0 </mrow>
    </md>.
    Luckily, <xref ref="th_int_root"/> helps us find an easy integer root of the polynomial on the left, allowing us to factor (in two steps) as 
    <me>
        x^3-3x+2=(x-1)(x^2+x-2)=(x-1)(x-1)(x+2)
    </me>.
    From the factored equation 
    <me>
        (x-1)(x-1)(x+2)=0
    </me>
    we conclude that the solutions to the original equation are <m>x=1</m> (a repeated root here) and <m>x=-2</m>.
    </p>
    </solution>
    </example>
    <p>
        Another important consequence of <xref ref="th_poly_roots"/> that we will make use of later in the course has to do with when two polynomials are equal (as functions), as articulated in <xref ref="cor_poly_equality"/>. In order to make sense of that result, we need to first make clear what we mean by two functions being equal. Recall our original intuitive idea of a function as a <em>rule</em> (or <em>assignment</em>) that assigns to each element of a specified input set (the domain) a unique output. When should we consider two such things to be the same? For one thing, they should have the same specified set of inputs (<ie/>, the same domain): this is condition (1) of <xref ref="d_function_equality"/>. Moreover, in order to represent the same rule (or assignment), each input should be assigned to the same output: this is condition (2) of <xref ref="d_function_equality"/>. 
    </p>
    <definition xml:id="d_function_equality">
    <title>Function equality</title>
    <statement>
    <p>
    Functions <m>f</m> and <m>g</m> are <term>equal</term>, denoted <m>f=g</m> if the following conditions hold: 
    <ol>
        <li>
            <p>
                <m>f</m> and <m>g</m> have the same domain <m>D</m>.
            </p>
        </li>
        <li>
            <p>
                <m>f(x)=g(x)</m> for all <m>x\in D</m>. 
            </p>
        </li>
    </ol>
    </p>
    </statement>
    </definition>
    <remark>
        <title>Function equality</title>
        <p>
            Note well the all important <q>for all</q> quantifier appearing in condition (2) of <xref ref="d_function_equality"/>. That detail makes the notion of <em>function equality</em> more nuanced than a simple <em>numeric equality</em> (like <m>5=2^2+1</m>).   Given two functions <m>f</m> and <m>g</m> with common domain <m>D</m>, the statement <m>f=g</m> is in fact a claim that a whole bunch of numeric equalities hold: namely, we have <m>f(x)=g(x)</m> for every <m>x\in D</m>. That's one equality for each <m>x\in D</m>. Since <m>D</m> is most often an infinite set (like an interval) for us, this means that <m>f=g</m> typically asserts that infinitely many numeric equalities hold. In this sense a function equality is very similar to the notion of an <em>identity</em> (like <m>(x+1)^2=x^2+2x+1</m>), which you may have encountered before.  
        </p>
    </remark>
    <p>
        Having made clear what we mean in general by function equality, we now look at the specific case of polynomial functions. <xref ref="cor_poly_equality"/> can be shown to be a consequence of <xref ref="th_poly_roots"/>, though we will omit that argument. 
    </p>
    <corollary xml:id="cor_poly_equality">
        <title>Polynomial equality</title>
        <statement>
            <p>
               Let 
               <md>
                <mrow>f(x) \amp =\anpoly \amp </mrow>
                <mrow>g(x) \amp =\bmpoly</mrow>
               </md> 
               be polynomials defined on a common infinite domain <m>D</m>, and assume <m>a_n</m> and <m>b_m</m> are both nonzero. We have <m>f=g</m> if and only if the following conditions hold: 
               <ol>
                <li>
                    <p>
                        <m>\deg f=\deg g</m> (<ie/>, <m>n=m</m>);
                    </p>
                </li>
                <li>
                    <p>
                        <m>a_k=b_k</m> for all <m>0\leq k\leq n</m>. 
                   </p>
                </li>
               </ol>
               In other words, two polynomials are equal as functions if and only if they have the same degree and coefficients. 
            </p>
        
        </statement>
    </corollary>
    </subsection>
    <subsection xml:id="ss_rational_funcs">
    <title>Rational functions</title>
    <p>
        Equipped now with some machinery for factoring polynomials, we can take a closer look at rational functions, which you will recall are just quotients of polynomials. In this section we will be interested only in determining the implied domain and zeros of a rational function. Before treating this specific family of functions, we first make official a simple observation about the domain and zeros of any function defined as a quotient of two functions.
    </p>
        <theorem xml:id="th_quot_func">
        <title>Quotients of functions</title>
        <statement>
        <p>
        Assume <m>f</m> is a function of the form 
        <me>
            f(x)=\frac{g(x)}{h(x)}
        </me>,
        where <m>g</m> and <m>h</m> are functions. 
        <ol>
            <li>
                <p>
                    The implied domain <m>D</m> of <m>f</m> is the set of <m>x\in \R</m> for which (a) <m>g(x)</m> and <m>h(x)</m> are both defined, and (b) <m>h(x)\ne 0</m>.
                </p>
            </li>
            <li>
                <p>
                    The set <m>Z</m> of zeros of <m>f</m> is the set of zeros of <m>g</m> that lie in <m>D</m>: <ie/>,
                    <me>
                        Z=\{x\in D\mid g(x)=0\}
                    </me>.
                    Equivalently, using logical shorthand:  
                    <men xml:id="eq_zeros_quot">
                        f(x)=0 \iff g(x)=0 \text{ and } h(x)\ne 0
                    </men>.
                </p>
            </li>
        </ol>
        </p>
        </statement>
        </theorem>
        <p>
            In the special case where <m>f(x)=p(x)/q(x)</m> is a quotient of two <em>polynomials</em>, the statements of <xref ref="th_quot_func"/> are simplified by the fact that <m>p(x)</m> and <m>q(x)</m> are defined everywhere, and have finitely many zeros.
        </p>
        <corollary xml:id="cor_rational_domain_zeros">
            <title>Domain and zeros of rational functions</title>
            <statement>
                <p>
                    Let <m>f(x)=p(x)/q(x)</m>, where <m>p</m> and <m>q</m> are polynomials. 
                    <ol>
                        <li>
                            <p>
                                The implied domain <m>D</m> of <m>f</m> is 
                                <me>
                                    D=\{x\in \R\mid q(x)\ne 0\}
                                </me>.
                            </p>
                        </li>
                        <li>
                            <p>
                                The set <m>Z</m> of zeros of <m>f</m> is the set of zeros of <m>p</m> that are not zeros of <m>q</m>: <ie/>, 
                                <me>
                                    Z=\{x\in \R\mid p(x)=0 \text{ and } q(x)\ne 0\}
                                </me>.
                                Equivalently, using logical shorthand: 
                                <men xml:id="eq_zeros_rational">
                                    f(x)=0 \iff p(x)=0 \text{ and } q(x)\ne 0
                                </men>.
                            </p>
                        </li>
                    </ol>
                </p>
            </statement>
        </corollary>
    <p>
        Since the domain and zeros of a rational function <m>f(x)=p(x)/q(x)</m> are determined by the zeros of <m>p</m> and <m>q</m>, and since by <xref ref="th_poly_roots"/> nonzero polynomials have only finitely many zeros (bounded in number by the polynomial's degree), it follows that <m>f</m> is defined everywhere except for the finitely many zeros of <m>q</m>, and <m>f</m> has finitely many zeros. 
    </p>
    <example xml:id="eg_rational_domain_zeros">
    <title>Rational functions: domain and zeros</title>
    <statement>
    <p>
    Compute the implied domain <m>D</m> and set of zeros <m>Z</m> for the given rational function. 
    <ol>
        <li>
            <p>
                <m>f(x)=\frac{x-3}{x^2-4x+3}</m>
            </p>
        </li>
        <li>
            <p>
                <m>g(x)=\frac{x^2-1}{x^2-4x+3}</m>
            </p>
        </li>
    </ol>
    </p>
    </statement>
    <solution>
    <p>
        <ol>
            <li>
                <p>
                    We have <m>f(x)=p(x)/q(x)</m>, where 
                    <md>
                        <mrow>p(x) \amp = x-3 \amp q(x)\amp =x^2-4x-3=(x-1)(x-3)</mrow>
                    </md>.
                    Since the zeros of <m>q</m> are <m>1</m> and <m>3</m>, the implied domain <m>D</m> of <m>f</m> is 
                    <me>
                        D=\R-\{1,3\}=(-\infty,1)\cup (1,3)\cup (3,\infty)
                    </me>.
                    The set of zeros <m>Z</m> of <m>f</m> is the set of <m>x</m> where <m>p(x)=0</m> and <m>q(x)\ne 0</m>. Since <m>p</m> is zero only at <m>x=3</m>, and since <m>3</m> is a zero of <m>q</m>, we conclude that <m>f</m> has no zeros: <ie/>, 
                    <me>
                        Z=\{\}=\emptyset
                    </me>.
                </p>
            </li>
            <li>
                <p>
                    We have <m>g(x)=p(x)/q(x)</m>, where 
                    <md>
                        <mrow>p(x) \amp = x^2-1=(x-1)(x+1) \amp q(x)\amp =x^2-4x-3=(x-1)(x-3)</mrow>
                    </md>.
                    Since the zeros of <m>q</m> are <m>1</m> and <m>3</m>, the implied domain <m>D</m> of <m>g</m> is 
                    <me>
                        D=\R-\{1,3\}=(-\infty,1)\cup (1,3)\cup (3,\infty)
                    </me>,
                    as above.
                    The set of zeros <m>Z</m> of <m>f</m> is the set of <m>x</m> where <m>p(x)=0</m> and <m>q(x)\ne 0</m>. Since <m>p</m> is zero at <m>1</m> and <m>-1</m>, and since <m>1</m> is a zero of <m>q</m>, we conclude that 1 is the only zero of <m>f</m>. 
                </p>
            </li>
        </ol>
    </p>
    </solution>
    </example>
    <p>
        In <xref ref="fig_rational_zeros_domain"/> you find graphs of the functions <m>f</m> and <m>g</m> from <xref ref="eg_rational_domain_zeros"/>. Consistent with our work in that example, the graphs indicate that <m>f</m> has no zeros and <m>g</m> has exactly one zero, at <m>x=-1</m>. 
    </p>
    <figure xml:id="fig_rational_zeros_domain">
        <caption>Graphs of rational functions</caption>
        <sidebyside valign="middle">
            <figure xml:id="fig_rational_func_a">
                <caption>Graph of <m>f(x)=\tfrac{x-3}{(x-1)(x-3)}</m></caption>
                <image>
            <shortdescription>Graph of rational function</shortdescription>
            <sageplot>
            f1=plot((x-3)/(x^2-4*x+3),x,-2,2.9, ymin=-4, ymax=4, detect_poles="yes")
            f2=plot((x-3)/(x^2-4*x+3),x,3.1,4, ymin=-4, ymax=4, detect_poles="yes")
            C=circle((3,1/2),.1)
            L=line(((1,-5),(1,5)),linestyle="dotted", color="red")
            f1+C+f2+L
            </sageplot>
        </image>
            </figure>
            <figure xml:id="fig_rational_func_b">
                <caption>Graph of <m>g(x)=\tfrac{(x-1)(x+1)}{(x-1)(x-3)}</m></caption>
                <image>
            <shortdescription>Graph of rational function</shortdescription>
            <sageplot>
            f1=plot((x^2-1)/(x^2-4*x+3),x,-2,.9, ymin=-6, ymax=6, detect_poles="yes")
            f2=plot((x^2-1)/(x^2-4*x+3),x,3.1,8, ymin=-6, ymax=6, detect_poles="yes")
            f3=plot((x^2-1)/(x^2-4*x+3),x,1.1,2.9, ymin=-5, ymax=5, detect_poles="yes")
            C=circle((1,-1),.1)
            P=point((-1,0),color="red",size=50)
            L=line(((3,-5),(3,5)),linestyle="dotted", color="red")
            f1+C+f2+L+f3+P
            </sageplot>
        </image>
            </figure>
        </sidebyside>
    </figure>
    <p>
        Furthermore, we saw in <xref ref="eg_rational_domain_zeros"/> that both functions have <m>D=\R-\{1,3\}</m> as their implied domain. Interestingly, the two graphs indicate that <m>1</m> and <m>3</m> are excluded from the domain in slightly different ways. 
        <ul>
            <li>
                <p>
                    The graph of <m>f</m> has what we will eventually call a <em>vertical asymptote</em> at <m>x=1</m>, and a <em>hole</em> at <m>x=3</m>.
                </p>
            </li>
            <li>
                <p>
                    The graph of <m>f</m> has a hole at <m>x=1</m> and a vertical asymptote at <m>x=3</m>. 
                </p>
            </li>
        </ul>
    The vertical asymptote behavior of the two graphs is something we will discuss in great detail later. For now we content ourselves to an explanation of the holes appearing in these graphs. Let's look only at 
    <me>
        f(x)=\frac{x-3}{x^2-4x+3}=\frac{x-3}{(x-3)(x-1)}
    </me>.
    Our analyis is somewhat involved, so we will break it up into a few steps. 
    <ul>
        <li>
            <p>
                Technically, <m>3</m> is not in the domain of <m>f</m>, and thus there should be no plotted point on the graph of <m>f</m> over <m>x=3</m>.
            </p>
        </li>
        <li>
            <p>However, for any <m>x\ne 3</m>, we may cancel the nonzero terms <m>x-3</m> in our expression above to get the simpler expression <m>1/(x-1)</m>. That is, we have 
            <me>
            x\ne 3 \implies f(x)=\frac{\cancel{x-3}}{\cancel{(x-3)}(x-1)}=\frac{1}{x-1}
            </me>.   
            </p>
        </li>
        <li>
            <p>
                Let <m>h(x)=\frac{1}{x-1}</m>. Technically <m>f</m> and <m>h</m> are not equal as functions, as is reflected in the fact that <m>f</m> has domain <m>\R-\{1,3\}</m> and <m>h</m> has domain <m>\R-\{1\}</m>. However, for all <m>x\ne 3</m>, we have <m>f(x)=h(x)</m>. 
            </p>
        </li>
        <li>
            <p>
                 Since <m>h(3)=1/2</m>, the point <m>P=(3,1/2)</m> appears on the graph of <m>h</m>. Furthermore, if we graph <m>h</m>, we see that points <m>(x,h(x))</m> on its graph approach <m>P=(3,1/2)</m> as <m>x</m> gets closer and closer to <m>3</m>. (As we will see later, this is a result of <m>h</m> being <em>continuous</em> at <m>x=3</m>.)  
            </p>
        </li>
        <li>
            <p>
                Lastly, since the graph of <m>f</m> agrees with the graph of <m>h</m> for all <m>x\ne 3</m>, points <m>(x,f(x))</m> on its graph <em>also</em> approach <m>(3,1/2)</m> as <m>x</m> gets closer and closer to <m>3</m>. As a result, the graph of <m>f</m> appears to approach the point <m>P=(3,1/2)</m>, but without this point being included on the graph itself. Hence the hole! 
            </p>
        </li>
    </ul>
    Well, that was a bit long winded! One amazing feature of calculus is its ability to express such cumbersome arguments in a much more economical manner. For example, using the language of <em>limits</em> (to be introduced soon),  our discussion above can be beautifully compressed into the following simple statement: 
    <me>
        \lim_{x\to 3}f(x)=h(3)=\frac{1}{2}
    </me>. 
    </p>
    </subsection>
</section>